{
  "name": "ai-agent-starter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply DB --remote"
  },
  "dependencies": {
    "openai": "^7.20.0"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
